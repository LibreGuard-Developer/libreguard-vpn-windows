<UserControl x:Class="LibreGuard_VPN_Desktop.Views.UpgradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:LibreGuard_VPN_Desktop.Converters"
             xmlns:models="clr-namespace:LibreGuard_VPN_Desktop.Models"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="900">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32">

            <!-- Header -->
            <StackPanel Margin="0,0,0,32">
                <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding GoBackCommand}" HorizontalAlignment="Left" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x2190;" FontFamily="Segoe UI Symbol, Segoe UI Emoji" Margin="0,0,8,0"/>
                        <TextBlock Text="Back to Settings"/>
                    </StackPanel>
                </Button>
                <TextBlock Text="Upgrade to Pro" Style="{StaticResource HeadingXLStyle}" Margin="0,0,0,8"/>
                    <TextBlock Text="Choose your preferred payment method" Style="{StaticResource MutedTextStyle}"/>
                </StackPanel>

                <!-- Billing Cycle Selection -->
                <StackPanel Visibility="{Binding IsPaymentMethodSelectionVisible, Converter={StaticResource BoolToVis}}" Margin="0,0,0,32">
                    <TextBlock Text="Billing Period" Style="{StaticResource HeadingStyle}" Margin="0,0,0,16" HorizontalAlignment="Center"/>
                    <Border Background="{DynamicResource MutedBrush}" CornerRadius="12" Padding="4" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Style="{StaticResource SegmentedButtonStyle}" 
                                         Content="Monthly ($5.99)" 
                                         IsChecked="{Binding SelectedCycle, Converter={StaticResource EqualToBool}, ConverterParameter={x:Static models:BillingCycle.Monthly}}"/>
                            <RadioButton Style="{StaticResource SegmentedButtonStyle}" 
                                         IsChecked="{Binding SelectedCycle, Converter={StaticResource EqualToBool}, ConverterParameter={x:Static models:BillingCycle.Yearly}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Yearly ($29.99)"/>
                                    <Border Background="#10B981" 
                                            CornerRadius="10" 
                                            Padding="8,4" 
                                            Margin="10,0,0,0" 
                                            VerticalAlignment="Center">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                        </Border.Effect>
                                        <TextBlock Text="SAVE ~30%" 
                                                   FontSize="10" 
                                                   FontWeight="ExtraBold" 
                                                   Foreground="White"/>
                                    </Border>
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Benefits Card -->
                <Border Style="{StaticResource CardStyle}" Background="{DynamicResource PrimaryLightBrush}" Margin="0,0,0,32" Visibility="{Binding IsPaymentMethodSelectionVisible, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="Pro Subscription Benefits" Style="{StaticResource HeadingStyle}" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,24"/>

                        <UniformGrid Columns="2">
                            <!-- Devices -->
                            <StackPanel Margin="0,0,16,20">
                                <TextBlock Text="&#x2713; 3 Active Devices" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                <TextBlock Text="Connect up to 3 devices simultaneously" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- Data -->
                            <StackPanel Margin="0,0,0,20">
                                <StackPanel Visibility="{Binding SelectedCycle, Converter={StaticResource EqualToVis}, ConverterParameter={x:Static models:BillingCycle.Yearly}}">
                                    <TextBlock Text="&#x2713; Unlimited Bandwidth" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Text="Unlimited bandwidth on Pro plan" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                                <StackPanel Visibility="{Binding SelectedCycle, Converter={StaticResource EqualToVis}, ConverterParameter={x:Static models:BillingCycle.Monthly}}">
                                    <TextBlock Text="&#x2713; Unlimited Bandwidth" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Text="Unlimited bandwidth on Pro plan" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Servers -->
                            <StackPanel Margin="0,0,16,20">
                                <TextBlock Text="&#x2713; Pro Server Access" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                <TextBlock Text="Access to all VPN servers including Pro tier" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- Custom Configs -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="&#x2713; Custom Configurations" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                <TextBlock Text="Access LibreGuard VPN over any other 3rd party VPN app (Advanced OpenVPN and IKEv2)" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- Duration -->
                            <StackPanel Margin="0,0,16,20">
                                <StackPanel Visibility="{Binding SelectedCycle, Converter={StaticResource EqualToVis}, ConverterParameter={x:Static models:BillingCycle.Yearly}}">
                                    <TextBlock Text="&#x2713; 365 Days Access" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Text="Full year of premium service" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                                <StackPanel Visibility="{Binding SelectedCycle, Converter={StaticResource EqualToVis}, ConverterParameter={x:Static models:BillingCycle.Monthly}}">
                                    <TextBlock Text="&#x2713; 30 Days Access" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Text="Full month of premium service" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Support -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="&#x2713; Priority Support" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,4"/>
                                <TextBlock Text="Faster response times for any issues" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </UniformGrid>

                        <Border Background="{DynamicResource StatusConnectedLightBrush}" CornerRadius="8" Padding="12" Margin="0,8,0,0" Visibility="{Binding SelectedCycle, Converter={StaticResource EqualToVis}, ConverterParameter={x:Static models:BillingCycle.Yearly}}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="&#x2605;" Foreground="{DynamicResource StatusConnectedBrush}" Margin="0,0,8,0"/>
                                <TextBlock Text="Best Value: Save ~30% compared to monthly billing" FontWeight="SemiBold" Foreground="{DynamicResource StatusConnectedBrush}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Payment Methods -->
            <UniformGrid Columns="2" Margin="0,0,0,32" Visibility="{Binding IsPaymentMethodSelectionVisible, Converter={StaticResource BoolToVis}}">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Pay with Card" Style="{StaticResource HeadingStyle}" Margin="0,0,0,8"/>
                        <TextBlock Text="Pay securely using your card" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,16"/>
                        <Button Content="Continue to Card Payment" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SelectCardCommand}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Monero (XMR)" Style="{StaticResource HeadingStyle}" Margin="0,0,0,8"/>
                        <TextBlock Text="Pay anonymously with Monero" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,16"/>
                        <Button Content="Pay with Monero" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SelectMoneroCommand}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Card Payment Screen -->
            <StackPanel Visibility="{Binding IsCardSelected, Converter={StaticResource BoolToVis}}">
                <Button Content="Change Payment Method" Style="{StaticResource GhostButtonStyle}" Command="{Binding SwitchPaymentMethodCommand}" HorizontalAlignment="Right" Margin="0,0,0,8"/>
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Complete your secure card payment" Style="{StaticResource HeadingStyle}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding CardPaymentStatusMessage}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,0,0,20"/>

                        <Border Background="{DynamicResource PrimaryLightBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Card Payment Status" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding CardPaymentStatus, TargetNullValue='Pending'}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                                <TextBlock Text="{Binding CardTransactionId, StringFormat={}Transaction: {0}}" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                            <Button Content="Open Checkout in App" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding OpenEmbeddedCheckoutCommand}" Margin="0,0,8,0"/>
                            <Button Content="Open in Browser" Style="{StaticResource GhostButtonStyle}" Command="{Binding OpenCheckoutInBrowserCommand}" Margin="8,0"/>
                            <Button Content="Check Status" Style="{StaticResource GhostButtonStyle}" Command="{Binding CheckCardPaymentStatusCommand}" Margin="8,0,0,0"/>
                        </StackPanel>

                        <Border Background="{DynamicResource StatusConnectedLightBrush}" CornerRadius="8" Padding="16" Visibility="{Binding IsPaymentComplete, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Payment confirmed. Your Pro account is active." FontWeight="SemiBold" Foreground="{DynamicResource StatusConnectedBrush}" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Monero Payment Screen -->
            <StackPanel Visibility="{Binding IsMoneroSelected, Converter={StaticResource BoolToVis}}">
                <Button Content="Change Payment Method" Style="{StaticResource GhostButtonStyle}" Command="{Binding SwitchPaymentMethodCommand}" HorizontalAlignment="Right" Margin="0,0,0,8"/>
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Payment expires in:" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding TimeRemaining}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,8,0,16"/>

                        <TextBlock Text="Amount to send:" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center"/>
                        <Grid HorizontalAlignment="Center" Margin="0,8,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding MoneroPrice.XmrAmount, StringFormat={}{0} XMR}" FontSize="32" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Copy" Style="{StaticResource GhostButtonStyle}" Command="{Binding CopyAmountCommand}" Margin="12,0,0,0" Padding="8,4" FontSize="12"/>
                        </Grid>
                        <TextBlock Text="{Binding MoneroPrice.UsdAmount, StringFormat={}~${0} USD}" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center" Margin="0,0,0,24"/>

                        <TextBlock Text="Send exactly the amount shown above to this address:" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8"/>
                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding MoneroInvoice.PaymentAddress}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="Copy" Style="{StaticResource GhostButtonStyle}" Command="{Binding CopyAddressCommand}" Margin="8,0,0,0"/>
                            </Grid>
                        </Border>

                        <TextBlock Text="Payment Status" Style="{StaticResource HeadingStyle}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding MoneroStatus.Status, TargetNullValue='Pending'}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" Margin="0,0,16,0" VerticalAlignment="Center">
                                <TextBlock Text="Received:" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding MoneroStatus.AmountReceived, StringFormat={}{0} XMR, TargetNullValue='0 XMR'}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,16,0" VerticalAlignment="Center">
                                <TextBlock Text="Required:" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding MoneroStatus.AmountRequired, StringFormat={}{0} XMR, TargetNullValue='0 XMR'}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Shortfall:" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Shortfall, StringFormat={}{0} XMR, TargetNullValue='0 XMR'}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Blockchain Confirmations" Style="{StaticResource HeadingStyle}" Margin="0,0,0,8"/>
                        <ProgressBar Minimum="0" Maximum="10" Value="{Binding MoneroStatus.Confirmations, TargetNullValue=0}" Height="12" Margin="0,0,0,8" Foreground="{DynamicResource PrimaryBrush}" Background="{DynamicResource PrimaryLightBrush}"/>
                        <TextBlock Text="{Binding MoneroStatus.Confirmations, StringFormat={}{0}/10 Confirmations, TargetNullValue='0/10 Confirmations'}" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center" Margin="0,0,0,16"/>

                        <Button Content="Check Payment Status" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CheckPaymentStatusCommand}" Margin="0,0,0,16"/>

                        <Border Background="{DynamicResource PrimaryLightBrush}" CornerRadius="8" Padding="16">
                            <StackPanel>
                                <TextBlock Text="Important Notes:" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,8"/>
                                <TextBlock Text="• Transaction verification takes around 20 minutes." Foreground="{DynamicResource PrimaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                <TextBlock Text="• Do not close this page until payment is confirmed." Foreground="{DynamicResource PrimaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                <TextBlock Text="• Send exactly the amount shown above." Foreground="{DynamicResource PrimaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
